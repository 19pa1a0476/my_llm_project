version: "3.9"
services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    depends_on:
      - worker

  worker:
    build: ./worker
    volumes:
      - ./data:/app/data
